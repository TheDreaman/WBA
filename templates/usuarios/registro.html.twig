{% extends 'base.html.twig' %}

{% block title %}Registro{% endblock %}

{% block body %}

<div class="container-fluid">
    <h1>Pantalla de registro</h1>

    {% if bolError is defined %}
        <h5>{{ bolError }}</h5>
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}
